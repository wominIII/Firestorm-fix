/**
 * @file fsmeshreconstructor.h
 * @brief Rebuild a DAE from mesh data already decoded by the viewer.
 */

#ifndef FS_MESH_RECONSTRUCTOR_H
#define FS_MESH_RECONSTRUCTOR_H

class FSMeshReconstructor
{
public:
    static void exportSelected();
};

#endif // FS_MESH_RECONSTRUCTOR_H
